package com.jobamax.appjobamax.enums

enum class FirebaseDynamicLinkHost(val host: String) {
    WISHLIST("wishlist"),
    CHAT("chat"),
    JOB_SEEKER_PROFILE("jobseekerProfile"),
    SOURCE("source"),
    JOB_SEARCH("jobSearch"),
}